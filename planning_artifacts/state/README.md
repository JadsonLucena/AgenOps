# Planning State Workspace

This directory stores ephemeral state snapshots while a task is in progress.

## Notes

- treated as transient workspace
- may be regenerated at any time
- canonical state lives under the shared state policy and versioning rules
- approved outputs should be promoted to durable docs when needed

# Usage guidance

Emit telemetry at the point of action:
- when an agent makes a decision
- when validation runs
- when an artifact moves between lifecycle states
- when a human approves or rejects a gate
- when the orchestrator merges canonical state

The telemetry layer is not just monitoring. It is the proof that governance is working under load.
